# Instraucciones para utilizar la configuración de este repositorio

Cuando se instala nvim en Debian, se crean por defecto las carpeta:

- ~/.config/nvim
- ~/.local/share/nvim

Elimina estas carpetas y crea un enlace simbólico con el mismo nombre que apunte a la carpeta "nvim" de este repositorio.
